{% extends 'AppBundle::base.html.twig' %}

{% block body -%}
    <h1>{{ 'client.labels.self'|trans }}  {{ 'crud.labels.new'|trans }}</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('client') }}">
            {{ 'Back to the list'|trans }}
        </a>
    </li>
</ul>
{% endblock %}
